"""Gramps ontology contribution (CONCEPT:AU-KG.ontology.federation-provider-leg).

Data-only subpackage: it carries a human-reviewed mapping of the public Gramps model
which the Agent Utilities hub discovers through the ontology-provider entry point. It
contains no instance extension, record, generated live schema, endpoint, or executable
graph-write path.
"""
