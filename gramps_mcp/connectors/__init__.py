"""Neutral Gramps source inputs for the central capability compiler.

This data-only package owns no endpoint, credential, instance mapping, live schema
fingerprint, signature, or graph-write implementation. Deployments compile and approve
these inputs centrally before GraphOS source synchronization is enabled.
"""
